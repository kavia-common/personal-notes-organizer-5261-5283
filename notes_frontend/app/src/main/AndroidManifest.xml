<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:label="@string/app_name"
        android:theme="@style/Theme.NotesApp"
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:supportsRtl="true">
        <activity
            android:name="org.example.app.ui.editor.NoteEditorActivity"
            android:exported="false"
            android:label="@string/title_edit_note" />
        <activity
            android:name="org.example.app.ui.folder.FolderNotesActivity"
            android:exported="false"
            android:label="@string/title_folder_notes" />
        <activity
            android:name="org.example.app.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
